{
  "name": "webgl2-instanced-particles",
  "version": "1.0.0",
  "description": "WebGL2 instanced rendering: iki yüz bin parçacık tek drawArraysInstanced çağrısında, vertexAttribDivisor ile örnek attribute'ları ve batching ile dürüst bayt karşılaştırması.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
